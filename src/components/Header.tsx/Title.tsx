import { Typography } from "@mui/material";

const Title = () => {
  return (
    <Typography
      variant="h3"
      component="h1"
      align="center"
      sx={{ fontFamily: "Italiano" }}
    >
      MyReads
    </Typography>
  );
};

export default Title;
