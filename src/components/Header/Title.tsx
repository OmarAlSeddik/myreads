import { Typography } from "@mui/material";

const Title = () => {
  return (
    <Typography
      variant="h4"
      color="primary"
      component="h1"
      align="center"
      fontFamily="Italiano"
    >
      MyReads
    </Typography>
  );
};

export default Title;
